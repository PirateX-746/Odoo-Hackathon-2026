# OS
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# Claude Code — personal/local settings, not shared with the team
.claude/settings.local.json

# Env files — secrets never get committed; each app ships its own .env.example
.env
.env.local
.env.*.local
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.log

# Dependency directories (defense-in-depth; frontend/ and backend/ also ignore these locally)
node_modules/

# Build outputs
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo
